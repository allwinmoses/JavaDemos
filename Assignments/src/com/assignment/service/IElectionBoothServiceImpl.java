//package com.assignment.service;
//
////public class IElectionBoothServiceImpl implements IElectionBoothService {
////    @Override
////    public boolean checkEligibility(int age, String locality, int voterId) throws UnderAgeExcepton, InvalidVoterException, LocalityNotFoundException {
////        ckeckAge(age);
////        checkEligibility(locality);
////        checkVoiterId(voterId);
////
////        return false;
////    }
////    public void checkAge(int age) throws UnderAgeException{
////        if (age<18){
////            throw new UnderAgeException("Age should be greater than 18");
////
////        }
////    }
////    public  void  checkLocality(String locality) throws LocalityNotFoundException{
////        String[] localities ={"Rajanagar", "jalahalli"};
////        boolean found = false;
////        for()
//    }
//}
